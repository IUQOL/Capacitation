{% extends 'SsCenterMainBundle:Master:master.html.twig' %}

{% block content -%}
    <br>
    <h1>{{ 'title'|trans([],'SsCenterBundle') }}</h1>
    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('ticket') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock content %}
